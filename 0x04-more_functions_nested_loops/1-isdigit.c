#include "main.h"

/**
 * _isdigit - a function that checks for a digit (0 through 9)
 * @c: an input integer
 * Return:1 if c is a igit or 0 otherwise
 */
int _isigit(int c)
{
	return (c >= 48 && c <= 57);
}
